# 1. RunPod Serverless 核心网关
runpod>=1.6.0

# 2. 深度学习上层核心库（移除 torch/torchvision 声明，直接沿用基础镜像中的原生高性能版本）
transformers>=4.44.0
diffusers>=0.30.0
accelerate>=0.33.0
peft>=0.12.0
safetensors>=0.4.1

# 3. 图像处理与编解码依赖
Pillow>=10.3.0

# 4. 文本 Tokenizer 必备底层依赖（防止大模型文本编码器冷启动报错）
sentencepiece
protobuf
